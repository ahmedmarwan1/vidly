@model Vidly_.Models.NewCustomerViewModel
@{
    ViewBag.Title = "New";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>New Customer</h2>
@using (@Html.BeginForm("Save", "Customer"))
{
    @Html.ValidationSummary(true,"Please Fix the following errors.")
    <div class="form-group">
        @Html.LabelFor(m=>m.customer.Name)
        @Html.TextBoxFor(m => m.customer.Name, new { @class="form-control"})
        @Html.ValidationMessageFor(m=>m.customer.Name)
    </div>
    <div>
        @Html.LabelFor(m => m.customer.MembershipTypeId)
        @Html.DropDownListFor(m => m.customer.MembershipTypeId, new SelectList(Model.MemberShipTypes, "Id", "Name"), "Select MemberShipType", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.customer.MembershipTypeId)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.customer.Birthdate)
        @Html.TextBoxFor(m => m.customer.Birthdate, new { @class = "form-control" })
        @Html.ValidationMessageFor(m=>m.customer.Birthdate)
    </div>
    <div class="checkbox">
        <label>
            @Html.CheckBoxFor(m=>m.customer.IsSubscribedToNewsletter)SubScribed To New Letter?
        </label>
    </div>
    @Html.HiddenFor(m=>m.customer.Id)
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}
@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}

